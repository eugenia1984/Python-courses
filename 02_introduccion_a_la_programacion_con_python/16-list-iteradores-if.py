# Ejemplo con LIST, ITERADORES e IF

lenguajes = ['Python', 'Kotlink', 'Java', 'JavaScript']

for lenguaje in lenguajes:
    if lenguaje == 'Python':
        print(lenguaje.upper())
    else: 
        print(lenguaje)
        